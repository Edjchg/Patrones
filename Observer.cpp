//
// Created by tony on 22/03/18.
//

#include "Observer.h"

list Observer::actualizarObserver(list lista) {
    if (! lista.empty()){
        for (int i = 0; i< lista.size();i++){
            return lista;
        }
    }
}

